export interface Service {
  slug: string;
  title: string;
  summary: string;
}

export interface PortfolioItem {
  slug: string;
  title: string;
  category: string;
  year: string;
  summary: string;
}

export interface PageCopy {
  title: string;
  intro: string;
}
